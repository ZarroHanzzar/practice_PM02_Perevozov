# Система валидации заказов - Тестовый фреймворк

## Описание
Тестовый фреймворк для валидации заказов. Содержит фальшивую реализацию валидатора и полный набор тестов.

## Установка

```bash
# Установка зависимостей
pip install pytest hypothesis freezegun pydantic pytz

# Или через requirements.txt
pip install -r requirements.txt